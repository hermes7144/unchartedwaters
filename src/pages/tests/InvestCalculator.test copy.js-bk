import { render } from '@testing-library/react';
import { Route } from 'react-router-dom';
import { withAllContextsInvest, withRouter } from '../../tests/utils';
import InvestCalculator from '../../components/InvestCalculator';

describe('Invest', () => {
  const fakelogin = jest.fn();
  const fakelogout = jest.fn();
  const fakeUser = jest.fn();

  afterEach(() => {
    fakelogin.mockReset();
    fakelogout.mockReset();
    fakeUser.mockReset();
  })
  it('renders correctly', () => {
    // 함수확인 필요
    // fakelogin.mockImplementation({ user: 'user' });
    // fakelogout.mockImplementation();
    // fakeUser.mockImplementation({ user: 'user' });
    render(
      withAllContextsInvest(
        withRouter(<Route path='/' element={<InvestCalculator />} />)
        , fakelogin, fakelogout, fakeUser
      )
    )
  })
})